spring:
    application:
        name: jobbot-dev
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/JobBot
        username: root
        password: password
    jpa:
        hibernate:
            ddl-auto: create-drop
        show-sql: false

logging:
    level:
        root: info