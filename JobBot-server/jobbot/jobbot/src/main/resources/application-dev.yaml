application:
    name: jobbot-dev
datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./data/JobBot
    username: dev
    password: password
jpa:
    hibernate:
        ddl-auto: drop-create
    show-sql: true

logging:
    level:
        root: debug